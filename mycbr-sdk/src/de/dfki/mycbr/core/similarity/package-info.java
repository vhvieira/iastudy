/**
 * Contains standard classes to maintain similarity functions for
 * attribute descriptions (<em>local similarity functions</em>) and concepts (<em>amalgamation functions</em>).

<p>
The model of the myCBR project allows definition of several similarity functions for an attribute description.
Each attribute description (concept) holds a collection of known similarity (amalgamation) functions. 
Each similarity value is an object of class <code>Similarity</code>.
</p>
 * @author myCBR Team
 * @since myCBR 3.0.0
 */
package de.dfki.mycbr.core.similarity;
